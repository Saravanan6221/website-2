pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {
                sh '''
                sudo rm -rf /var/www/site2/*
                sudo cp -r Zip-EBS-AWS/* /var/www/site2/
                '''
            }
        }
    }
}
